import 'package:flutter/material.dart';

class FarmTab extends StatelessWidget {
  const FarmTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Ventana de granja")),
    );
  }
}
