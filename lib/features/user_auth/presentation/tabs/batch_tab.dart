import 'package:flutter/material.dart';

class BatchTab extends StatelessWidget {
  const BatchTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Ventana de lotes")),
    );
  }
}
